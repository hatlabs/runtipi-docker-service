FROM debian:trixie-slim

# Install Debian packaging tools
RUN apt-get update && \
    apt-get install -y \
        build-essential \
        debhelper \
        devscripts \
        curl \
        ca-certificates \
        && rm -rf /var/lib/apt/lists/*

WORKDIR /build
